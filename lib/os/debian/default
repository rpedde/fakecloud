# -*- mode: shell-script -*-

function valid_images() {
    _RETVAL=""
    for release in squeeze etch; do
	_RETVAL+="debian-${release} "
    done
}

export mirror=${DEBIAN_MIRROR:-"http://ftp.us.debian.org"}